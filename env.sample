# ========================================
# Environment Variables Sample File
# ========================================
# Copy this file to .env and update the values as needed
# cp env.sample .env

# ========================================
# Server Configuration
# ========================================

# Server port (default: 3001)
PORT=3001

# ========================================
# JWT Authentication
# ========================================

# JWT secret key for authentication (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ========================================
# Environment Mode
# ========================================

# Environment mode: development or production
# - development: Uses local database and development settings
# - production: Uses production database path and settings
NODE_ENV=development

# ========================================
# Database Configuration
# ========================================

# Database path is automatically determined based on NODE_ENV:
# - Development: ./backend/db/database.db
# - Production: %APPDATA%/EyercallData/inventory.db (Windows)
#               ~/Library/Application Support/EyercallData/inventory.db (macOS)
#               ~/.local/share/EyercallData/inventory.db (Linux)

# ========================================
# CORS Configuration
# ========================================

# CORS origin is automatically set based on NODE_ENV:
# - Development: http://localhost:5173 (Vite dev server)
# - Production: file:// (Electron app)

# ========================================
# System Paths (Auto-detected)
# ========================================

# These are automatically detected by the system:
# - APPDATA: Windows application data directory
# - USERPROFILE: Windows user profile directory
# - HOME: User home directory (macOS/Linux)
# - XDG_DATA_HOME: Linux XDG data directory

# ========================================
# Development Notes
# ========================================

# For development:
# - Set NODE_ENV=development
# - Database will be created in ./backend/db/database.db
# - CORS will allow localhost:5173

# For production:
# - Set NODE_ENV=production
# - Set a strong JWT_SECRET
# - Database will be created in the appropriate system directory
# - CORS will be set to file:// for Electron

# ========================================
# Security Recommendations
# ========================================

# 1. Always change JWT_SECRET in production
# 2. Use a strong, random string for JWT_SECRET
# 3. Keep your .env file secure and never commit it to version control
# 4. Consider using environment-specific .env files (.env.development, .env.production)

# ========================================
# Example Production Values
# ========================================

# NODE_ENV=production
# JWT_SECRET=your-very-long-and-random-secret-key-here
# PORT=3001 